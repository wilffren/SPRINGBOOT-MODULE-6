spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hexagonal_db?useSSL=false&serverTimezone=UTC
    username: root
    password: Qwe.123*
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

logging:
  level:
    com.example: DEBUG